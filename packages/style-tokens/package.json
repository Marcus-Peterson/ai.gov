{
  "name": "@repo/style-tokens",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "type": "module",
  "files": [
    "dist",
    "properties"
  ],
  "scripts": {
    "build": "tsc && style-dictionary build --config ./dist/config.js",
    "clean": "rm -rf build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@divriots/style-dictionary-to-figma": "^0.4.0",
    "@repo/typescript-config": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "@vitest/coverage-istanbul": "^3.0.7",
    "style-dictionary": "^4.4.0",
    "vitest": "^3.0.7"
  }
}
